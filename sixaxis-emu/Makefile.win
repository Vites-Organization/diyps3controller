BINS=emuclient
CC=gcc
CFLAGS=-Wall
LIB=-lmingw32 -lSDLmain -lSDL -llibxml2 -lpthread -lws2_32
INC=

all: $(BINS)

clean:
	rm -f $(BINS) *~ *.o

emuclient: emuclient.o sixaxis.o dump.o macros.o config.o conversion.o config_writter.o config_reader.o sdl_tools.o win_serial_con.o calibration.o
	$(CC) -o $@ $^ $(LIB)

%.o: %.c
	$(CC) $(CFLAGS) $(INC) -o $@ -c $< 
